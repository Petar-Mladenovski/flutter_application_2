name: flutter_application_2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2

  #FIREBASE
  firebase_core: ^3.8.1 # https://pub.dev/packages/firebase_core
  firebase_auth: ^5.3.4 # https://pub.dev/packages/firebase_auth
  image_picker: ^1.1.2
  firebase_storage: ^12.3.7
  path: ^1.9.0
  shared_preferences: ^2.3.4 # https://pub.dev/packages/shared_preferences


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
